<!DOCTYPE html>
<html lang="pt-BR">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>GN Marketing - Nossos Clientes</title>
    <link href="https://fonts.googleapis.com/css2?family=Inter:wght@300;400;500;600;700;800&family=Nunito+Sans:wght@300;400;600;700&display=swap" rel="stylesheet">
    <style>
        * { margin: 0; padding: 0; box-sizing: border-box; }
        
        body {
            font-family: 'Nunito Sans', sans-serif;
            line-height: 1.6;
            color: #3D2914;
            background: #F5F2ED;
        }
        
        h1, h2, h3 {
            font-family: 'Inter', sans-serif;
            font-weight: 700;
            letter-spacing: -0.5px;
        }
        
        .container { max-width: 1200px; margin: 0 auto; padding: 0 20px; }
        
        /* Header */
        .header {
            background: linear-gradient(135deg, #F0EBE5 0%, #F5F2ED 100%);
            padding: 80px 0;
            text-align: center;
        }
        
        .header h1 {
            font-size: 3.2rem;
            margin-bottom: 30px;
            color: #3D2914;
            font-weight: 800;
            letter-spacing: -1px;
        }
        
        .highlight { color: #A0755E; }
        
        .header p {
            font-size: 1.3rem;
            color: #6B4E3D;
            max-width: 600px;
            margin: 0 auto;
        }
        
        /* Gallery */
        .gallery {
            padding: 80px 0;
            background: white;
        }
        
        .gallery-wrapper {
            display: flex;
            gap: 30px;
            overflow-x: auto;
            scroll-behavior: smooth;
            scrollbar-width: none;
            -ms-overflow-style: none;
            padding: 20px 0;
        }
        
        .gallery-wrapper::-webkit-scrollbar {
            display: none;
        }
        
        .client-screenshot {
            flex: 0 0 auto;
            width: 350px;
            height: 600px;
            border-radius: 20px;
            box-shadow: 0 15px 35px rgba(160, 117, 94, 0.3);
            overflow: hidden;
            transition: transform 0.3s ease;
        }
        
        .client-screenshot:hover {
            transform: translateY(-10px);
        }
        
        .client-screenshot img {
            width: 100%;
            height: 100%;
            object-fit: cover;
        }
        
        /* Navigation */
        .gallery-nav {
            text-align: center;
            margin-top: 40px;
        }
        
        .nav-button {
            background: #A0755E;
            color: white;
            border: none;
            width: 60px;
            height: 60px;
            border-radius: 50%;
            margin: 0 15px;
            cursor: pointer;
            font-size: 1.5rem;
            transition: all 0.3s ease;
        }
        
        .nav-button:hover {
            background: #8B6F47;
            transform: scale(1.1);
        }
        
        /* Footer */
        .footer {
            background: #F0EBE5;
            padding: 40px 0;
            text-align: center;
            color: #6B4E3D;
            margin-top: 40px;
        }
        
        /* Responsive */
        @media (max-width: 768px) {
            .header h1 { font-size: 2.5rem; }
            .client-screenshot { width: 300px; height: 550px; }
            .gallery { padding: 40px 0; }
        }
    </style>
</head>
<body>
    <section class="header">
        <div class="container">
            <h1>Nossos <span class="highlight">clientes</span></h1>
            <p>Perfis que transformamos com estratégias de conteúdo eficazes</p>
        </div>
    </section>

    <section class="gallery">
        <div class="container">
            <div class="gallery-wrapper" id="gallery">
                
                <div class="client-screenshot">
                    <img src="https://i.imgur.com/LWXzcnN.jpeg" alt="Cliente 1">
                </div>

                <div class="client-screenshot">
                    <img src="https://i.imgur.com/WN7TMuZ.jpeg" alt="Cliente 2">
                </div>

                <div class="client-screenshot">
                    <img src="https://i.imgur.com/D2tI1O8.jpeg" alt="Cliente 3">
                </div>

                <div class="client-screenshot">
                    <img src="https://i.imgur.com/9xPa8jp.jpeg" alt="Cliente 4">
                </div>

                <div class="client-screenshot">
                    <img src="https://i.imgur.com/aGhg7hk.jpeg" alt="Cliente 5">
                </div>

                <div class="client-screenshot">
                    <img src="https://i.imgur.com/IQJTIhO.jpeg" alt="Cliente 6">
                </div>

                <div class="client-screenshot">
                    <img src="https://i.imgur.com/YPkbOr0.jpeg" alt="Cliente 7">
                </div>

                <div class="client-screenshot">
                    <img src="https://i.imgur.com/CjE4prs.jpeg" alt="Cliente 8">
                </div>

                <div class="client-screenshot">
                    <img src="https://i.imgur.com/rruLlQq.jpeg" alt="Cliente 9">
                </div>

                <div class="client-screenshot">
                    <img src="https://i.imgur.com/JNVn2MJ.jpeg" alt="Cliente 10">
                </div>

                <div class="client-screenshot">
                    <img src="https://i.imgur.com/BKRlzek.jpeg" alt="Cliente 11">
                </div>

                <div class="client-screenshot">
                    <img src="https://i.imgur.com/7R74oX6.jpeg" alt="Cliente 12">
                </div>

                <div class="client-screenshot">
                    <img src="https://i.imgur.com/gFYLP3w.jpeg" alt="Cliente 13">
                </div>

            </div>

            <div class="gallery-nav">
                <button class="nav-button" onclick="scrollGallery(-1)">‹</button>
                <button class="nav-button" onclick="scrollGallery(1)">›</button>
            </div>
        </div>
    </section>

    <div class="footer">
        <p>&copy; 2025 GN Marketing. Transformando empresas em autoridades digitais.</p>
    </div>

    <script>
        function scrollGallery(direction) {
            const gallery = document.getElementById('gallery');
            const cardWidth = 380;
            const scrollAmount = cardWidth * direction;
            
            gallery.scrollBy({
                left: scrollAmount,
                behavior: 'smooth'
            });
        }

        // Mobile swipe
        let startX;
        const gallery = document.getElementById('gallery');

        gallery.addEventListener('touchstart', e => {
            startX = e.touches[0].clientX;
        });

        gallery.addEventListener('touchmove', e => {
            if (!startX) return;
            
            let currentX = e.touches[0].clientX;
            let diffX = startX - currentX;
            
            if (Math.abs(diffX) > 50) {
                if (diffX > 0) {
                    scrollGallery(1);
                } else {
                    scrollGallery(-1);
                }
                startX = null;
            }
        });
    </script>
</body>
</html>
